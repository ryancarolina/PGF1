using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConklinRyan_UnitySetup : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello PGF!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
